import { Routes } from '@angular/router';
import { CatalogComponent } from '../features/catalog/catalog.component';

export const CATALOG_ROUTE: Routes = [
  {
    path: '',
    component: CatalogComponent,
  },
];
